# 📰 Fake News Prediction

A machine learning project designed to detect and classify fake news using **Natural Language Processing (NLP)** techniques and supervised learning algorithms. The system analyzes text data to determine whether a given news article is *real* or *fake* based on linguistic and statistical features.

---

## 🚀 Features
- Text preprocessing and cleaning (stopword removal, stemming, etc.)
- Feature extraction using **TF-IDF Vectorization**
- Model training using **Logistic Regression**, **Naive Bayes**, or **Random Forest**
- Accuracy evaluation and confusion matrix visualization
- User-friendly interface (if implemented) for testing new headlines

---

## 🧠 Technologies Used
- **Python**
- **NumPy**, **Pandas**, **Matplotlib**, **Seaborn**
- **Scikit-learn**
- **NLTK** for text preprocessing

---

